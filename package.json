{
  "name": "happy-brain-damage-24-7",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx firebase deploy --only hosting",
    "deploy:ci": "vite build && npx firebase deploy --only hosting --non-interactive",
    "seed": "tsx scripts/seed.ts",
    "emulators": "npx firebase emulators:start --only firestore,auth --import=./.emulator-data --export-on-exit"
  },
  "dependencies": {
    "firebase": "^12.2.1",
    "framer-motion": "^11.3.8",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "zustand": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "autoprefixer": "^10.4.20",
    "firebase-tools": "^13.16.0",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.10",
    "tsx": "^4.19.1",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
